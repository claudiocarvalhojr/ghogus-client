<% if (!users || users.length == 0) { %>
    <p>Nenhum usuário cadastrado!</p>
<% } else { users.forEach(function(user) { %>
    <p>Nome: <%= user.firstName %> | Sobrenome: <%= user.lastName %> | Nascimento: <%= user.birthDate %> | E-mail: <%= user.email %> | Telefone: <%= user.phoneNumber %></p>
<% })} %>